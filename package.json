{
  "name": "lifeboat-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client": "npm run dev --prefix frontend",
    "server": "npm run dev --prefix backend",
    "dev": "concurrently \"npm run client\" \"npm run server\" --kill-others --names \"FRONTEND,BACKEND\" --prefix-colors \"cyan,green\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
